# SQLite. Versions 3.8.0 and up are supported.
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem "sqlite3"
#
default: &default
  adapter: trilogy
  encoding: utf8mb4
  port: 3306
  max_connections: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  <<: *default
  host: <%= Rails.application.credentials.dev.db_host %>
  database: <%= Rails.application.credentials.dev.db_name %>
  username: <%= Rails.application.credentials.dev.db_username %>
  password: <%= Rails.application.credentials.dev.db_password %>

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  adapter: trilogy
  host: <%= ENV.fetch('TEST_DB_HOST', '127.0.0.1') %>
  port: <%= ENV.fetch('TEST_DB_PORT', 3306) %>
  database: <%= ENV.fetch('TEST_DB_DATABASE', 'test_db') %>
  username: <%= ENV.fetch('TEST_DB_USERNAME', 'test') %>
  password: <%= ENV.fetch('TEST_DB_PASSWORD', 'testpassword') %>

# Store production database in the storage/ directory, which by default
# is mounted as a persistent Docker volume in config/deploy.yml.
production:
  primary:
    <<: *default
    host: <%= ENV.fetch('DB_HOST', 'localhost') %>
    database: auth_production
    username: root
    password: <%= ENV.fetch('MYSQL_ROOT_PASSWORD') %>
  cache:
    <<: *default
    host: <%= ENV.fetch('DB_HOST', 'localhost') %>
    database: auth_cache
    username: root
    password: <%= ENV.fetch('MYSQL_ROOT_PASSWORD') %>
    migrations_paths: db/cache_migrate
  queue:
    <<: *default
    host: <%= ENV.fetch('DB_HOST', 'localhost') %>
    database: auth_queue
    username: root
    password: <%= ENV.fetch('MYSQL_ROOT_PASSWORD') %>
    migrations_paths: db/queue_migrate
  cable:
    <<: *default
    host: <%= ENV.fetch('DB_HOST', 'localhost') %>
    database: auth_cable
    username: root
    password: <%= ENV.fetch('MYSQL_ROOT_PASSWORD') %>
    migrations_paths: db/cable_migrate
